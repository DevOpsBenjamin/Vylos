name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      # Build showcase landing page
      - name: Build pages
        run: pnpm build:pages

      # Build each demo project
      - name: Build demos
        run: pnpm build:demos

      # Assemble _site/
      - name: Assemble site
        run: |
          mkdir -p _site
          cp -r pages/dist/* _site/
          cp -r projects/1-basic/dist _site/1-basic
          cp -r projects/2-advanced/dist _site/2-advanced
          cp -r projects/3-phone/dist _site/3-phone

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
