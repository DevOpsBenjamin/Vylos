import type { VylosLocation } from '@vylos/core';

const outside: VylosLocation = {
  id: 'outside',
  name: 'Outside',
  backgrounds: [{ path: '/locations/outside/assets/outside.png' }],
};

export default outside;
