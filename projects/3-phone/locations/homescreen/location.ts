import type { VylosLocation } from '@vylos/core';

const homescreen: VylosLocation = {
  id: 'homescreen',
  name: 'Home',
  backgrounds: [{ path: '' }], // Phone wallpaper handled by component
};

export default homescreen;
